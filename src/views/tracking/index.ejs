<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Track Your Shipment - Atlantic Shipping</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .tracking-container {
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
        }
        .logo {
            max-width: 200px;
            margin-bottom: 30px;
        }
        .tracking-box {
            background: white;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
            padding: 40px;
            text-align: center;
        }
        .tracking-input {
            max-width: 500px;
            margin: 0 auto;
        }
    </style>
</head>
<body>
    <div class="tracking-container">
        <div class="text-center">
            <img src="/logo.png" alt="Atlantic Shipping" class="logo">
        </div>

        <div class="tracking-box">
            <h1 class="h3 mb-4">Track Your Shipment</h1>

            <% if (error && error.length > 0) { %>
                <div class="alert alert-danger" role="alert">
                    <%= error %>
                </div>
            <% } %>

            <% if (success && success.length > 0) { %>
                <div class="alert alert-success" role="alert">
                    <%= success %>
                </div>
            <% } %>

            <div class="tracking-input">
                <form action="/tracking/track" method="GET" class="mb-4" id="trackingForm">
                    <div class="input-group">
                        <input type="text" class="form-control form-control-lg" 
                               name="trackingNumber" id="trackingNumber"
                               placeholder="Enter your tracking number"
                               required>
                        <button class="btn btn-primary btn-lg" type="submit">
                            <i class="fas fa-search me-2"></i>Track
                        </button>
                    </div>
                </form>

                <p class="text-muted">
                    Enter your tracking number to get real-time updates on your shipment.
                </p>
            </div>
        </div>

        <div class="text-center mt-4">
            <p class="text-muted">
                Need help? Contact us at 
                <a href="mailto:operations@atlanticshippings.com">operations@atlanticshippings.com</a>
            </p>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.getElementById('trackingForm').addEventListener('submit', function(e) {
            e.preventDefault();
            const trackingNumber = document.getElementById('trackingNumber').value.trim();
            if (trackingNumber) {
                window.location.href = '/tracking/track/' + trackingNumber;
            }
        });
    </script>
</body>
</html>
